#hosts file for defining, which hosts the playbook should be run on
--- 
all:
  hosts:
      cartman:
        ansible_port: 22
        ansible_host: cartmanAnsibleAdmin
        ansible_ssh_common_args: '-F /mnt/c/Users/Tom-Erik/.ssh/config'
